name: mobile_baha
description: "Veylo - anonymous dating app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.2
  firebase_messaging: ^15.1.3
  firebase_database: ^11.1.4
  go_router: ^14.3.0
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  image: ^4.1.7
  google_mlkit_selfie_segmentation: ^0.10.1
  flutter_sound: ^9.14.2          # ← голосовые
  permission_handler: ^11.3.1     # ← микрофон
    # ← запрет скриншотов
  path_provider: ^2.1.4           # ← временные файлы аудио

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  rename_app: ^1.6.5

flutter:
  uses-material-design: true